{
  "password-field": {
    "input-transition":      { "$type": "other",      "$value": "0.15s opacity cubic-bezier(0.4, 0, 0.2, 1)", "$description": "Input opacity transition (show/hide animation)" },

    "toggle-icon": {
      "cursor":              { "$type": "other",      "$value": "pointer",                  "$description": "Cursor on the show/hide toggle wrapper" },
      "opacity":             { "$type": "number",     "$value": 1,                          "$description": "Default opacity of the toggle icon" }
    },

    "details": {
      "padding-inline":      { "$type": "dimension",  "$value": "{space.4}",                "$description": "padding-inline of the details slot — was 16px" }
    },

    "infos": {
      "padding":             { "$type": "dimension",  "$value": "{space.0}",                "$description": "Padding of the info popup row — was '0 10px'; block=0, inline handled below" },
      "padding-inline":      { "$type": "dimension",  "$value": "{space.3}",                "$description": "Inline padding of the info popup — was 10px; nearest token space.3=12px" }
    },

    "item": {
      "padding":             { "$type": "dimension",  "$value": "{space.2}",                "$description": "Padding of each requirement tile — was 8px" },
      "text-align":          { "$type": "other",      "$value": "center",                   "$description": "Text alignment inside requirement tile" },
      "position":            { "$type": "other",      "$value": "relative",                 "$description": "Position for the absolute validate checkmark" },
      "overflow":            { "$type": "other",      "$value": "visible",                  "$description": "Overflow so validate badge is visible outside the tile" }
    },

    "item--valid": {
      "icon-opacity":        { "$type": "number",     "$value": 1,                          "$description": "Icon opacity when requirement is satisfied" }
    },

    "item-icon": {
      "opacity":             { "$type": "number",     "$value": 0.2,                        "$description": "Default icon opacity when requirement is not yet satisfied" },
      "font-size":           { "$type": "dimension",  "$value": "45px",                     "$description": "Glyph font-size — intentionally larger than font.size.5xl (2.25rem = 36px); kept as literal 45px. Arbitrage: token spécifique retenu, font.size.5xl insuffisant" },
      "font-weight":         { "$type": "fontWeight", "$value": "{font.weight.bold}",        "$description": "Glyph font-weight — bold makes the character icon visually heavier" }
    },

    "validate": {
      "position":            { "$type": "other",      "$value": "absolute",                 "$description": "Checkmark is positioned absolute relative to .origam-password-field__item" },
      "top":                 { "$type": "dimension",  "$value": "{space.0}",                "$description": "Top anchor for the validate badge" },
      "right":               { "$type": "dimension",  "$value": "{space.0}",                "$description": "Right anchor for the validate badge" },
      "transform":           { "$type": "other",      "$value": "translate(50%, -50%)",     "$description": "Transform to center the badge on the tile corner" }
    }
  }
}
