# Node / build artefacts
node_modules
dist
dist-ssr
docs/.vitepress/dist
docs/.vitepress/cache
coverage

# VCS / IDE / agent metadata
.git
.gitignore
.github
.idea
.vscode
.claude
.husky

# Local env
.env
.env.*
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# Tests output
tests/e2e/.results
tests/e2e/.report
cypress/screenshots
cypress/videos

# OS
.DS_Store
Thumbs.db

# Docker / CI scaffolding (avoid recursive copies)
docker
.dockerignore
Dockerfile*

# Long-form docs not needed in the image
ROADMAP.md
CLAUDE.md
CLAUDE_CODE_BRIEF.md
# NOTE: CHANGELOG.md is consumed at build-time by
# packages/marketing/scripts/parse-changelog.mjs (generates
# .generated/changelog.json which Nitro imports during prerender).
# Do NOT add it back here unless you also stop parsing it in the
# marketing build.

# Misc build artefacts tracked by .gitignore as well
*.tsbuildinfo
maquettes
figma-plugin
