/** Relative path (from marketing root) where docs static build is served. */
export const STATIC_DOCS_OUTPUT_RELATIVE = 'public/docs' as const

/** Relative path (from marketing root) where stories static build is served. */
export const STATIC_STORIES_OUTPUT_RELATIVE = 'public/stories' as const

/** Alias kept for Nitro server consumption via sendFromDir. */
export const STATIC_DOCS_DIR = 'public/docs' as const

/** Alias kept for Nitro server consumption via sendFromDir. */
export const STATIC_STORIES_DIR = 'public/stories' as const
