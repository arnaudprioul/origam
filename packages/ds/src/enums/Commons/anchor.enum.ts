export enum BLOCK {
    TOP = 'top',
    BOTTOM = 'bottom',
}

// TODO - rework inline
export enum INLINE {
    LEFT = 'left',
    RIGHT = 'right'
}

export enum START_END {
    START = 'start',
    END = 'end',
}