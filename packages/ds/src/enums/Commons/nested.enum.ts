export enum SELECT_STRATEGY {
    SINGLE_LEAF = 'single-leaf',
    LEAF = 'leaf',
    INDEPENDENT = 'independent',
    SINGLE_INDEPENDENT = 'single-independent',
    CLASSIC = 'classic'
}

export enum OPEN_STRATEGY {
    SINGLE = 'single',
    MULTIPLE = 'multiple',
    LIST = 'list'
}

export enum SELECTED {
    ON = 'on',
    OFF = 'off',
    INDETERMINATE = 'indeterminate'
}
