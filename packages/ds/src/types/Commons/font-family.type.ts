/**
 * Font-family token keys (map to `--origam-font__family---{key}`):
 *   sans (Inter / Helvetica Neue / Arial) · mono (JetBrains Mono / Fira Code) · serif (Georgia / Times New Roman)
 */
export type TFontFamily = 'sans' | 'mono' | 'serif'
