import type { TAlign } from '../../types'

export interface IAlignProps {
    align?: TAlign,
    alignSm?: TAlign,
    alignMd?: TAlign,
    alignLg?: TAlign,
    alignXl?: TAlign,
    alignXxl?: TAlign
}
