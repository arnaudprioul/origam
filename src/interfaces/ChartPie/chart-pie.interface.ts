import type { IChartBaseProps } from '../../interfaces'

/**
 * Props for `<OrigamChartPie>` — pie chart
 *
 * Type-specific wrapper around `<OrigamChart type="pie">`
 * with a tightly-typed surface (no `type` prop, only options
 * relevant to pie charts).
 */
export interface IChartPieProps extends IChartBaseProps {

}
