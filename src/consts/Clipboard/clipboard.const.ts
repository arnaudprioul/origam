/**
 * Default feedback window (ms) the "Copied!" pill stays visible after
 * a successful `useClipboard()` write. Aligned on the Snackbar default
 * so the visual rhythm stays consistent across the DS.
 */
export const CLIPBOARD_DEFAULT_FEEDBACK_DURATION_MS = 2000
